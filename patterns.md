# Patterns

- Preserve durable semantic state; omit chronology, repeated reasoning, and Git-reconstructable facts.
- Use explicit extraction templates as semantic compression skeletons.
- Separate restart/handoff state from canonicalization recommendations.